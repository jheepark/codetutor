"use strict";
module.exports = function(Promise) {
let SomePromiseArray = Promise._SomePromiseArray;
function any(promises) {
    let ret = new SomePromiseArray(promises);
    let promise = ret.promise();
    ret.setHowMany(1);
    ret.setUnwrap();
    ret.init();
    return promise;
}

Promise.any = function (promises) {
    return any(promises);
};

Promise.prototype.any = function () {
    return any(this);
};

};
