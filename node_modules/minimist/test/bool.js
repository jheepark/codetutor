let parse = require('../');
let test = require('tape');

test('flag boolean default false', function (t) {
    let argv = parse(['moo'], {
        boolean: ['t', 'verbose'],
        default: { verbose: false, t: false }
    });
    
    t.deepEqual(argv, {
        verbose: false,
        t: false,
        _: ['moo']
    });
    
    t.deepEqual(typeof argv.verbose, 'boolean');
    t.deepEqual(typeof argv.t, 'boolean');
    t.end();

});

test('boolean groups', function (t) {
    let argv = parse([ '-x', '-z', 'one', 'two', 'three' ], {
        boolean: ['x','y','z']
    });
    
    t.deepEqual(argv, {
        x : true,
        y : false,
        z : true,
        _ : [ 'one', 'two', 'three' ]
    });
    
    t.deepEqual(typeof argv.x, 'boolean');
    t.deepEqual(typeof argv.y, 'boolean');
    t.deepEqual(typeof argv.z, 'boolean');
    t.end();
});
test('boolean and alias with chainable api', function (t) {
    let aliased = [ '-h', 'derp' ];
    let regular = [ '--herp',  'derp' ];
    let opts = {
        herp: { alias: 'h', boolean: true }
    };
    let aliasedArgv = parse(aliased, {
        boolean: 'herp',
        alias: { h: 'herp' }
    });
    let propertyArgv = parse(regular, {
        boolean: 'herp',
        alias: { h: 'herp' }
    });
    let expected = {
        herp: true,
        h: true,
        '_': [ 'derp' ]
    };
    
    t.same(aliasedArgv, expected);
    t.same(propertyArgv, expected); 
    t.end();
});

test('boolean and alias with options hash', function (t) {
    let aliased = [ '-h', 'derp' ];
    let regular = [ '--herp', 'derp' ];
    let opts = {
        alias: { 'h': 'herp' },
        boolean: 'herp'
    };
    let aliasedArgv = parse(aliased, opts);
    let propertyArgv = parse(regular, opts);
    let expected = {
        herp: true,
        h: true,
        '_': [ 'derp' ]
    };
    t.same(aliasedArgv, expected);
    t.same(propertyArgv, expected);
    t.end();
});

test('boolean and alias using explicit true', function (t) {
    let aliased = [ '-h', 'true' ];
    let regular = [ '--herp',  'true' ];
    let opts = {
        alias: { h: 'herp' },
        boolean: 'h'
    };
    let aliasedArgv = parse(aliased, opts);
    let propertyArgv = parse(regular, opts);
    let expected = {
        herp: true,
        h: true,
        '_': [ ]
    };

    t.same(aliasedArgv, expected);
    t.same(propertyArgv, expected); 
    t.end();
});

// regression, see https://github.com/substack/node-optimist/issues/71
test('boolean and --x=true', function(t) {
    let parsed = parse(['--boool', '--other=true'], {
        boolean: 'boool'
    });

    t.same(parsed.boool, true);
    t.same(parsed.other, 'true');

    parsed = parse(['--boool', '--other=false'], {
        boolean: 'boool'
    });
    
    t.same(parsed.boool, true);
    t.same(parsed.other, 'false');
    t.end();
});
